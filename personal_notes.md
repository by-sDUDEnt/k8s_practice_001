# notes for learning 

1. Make persistent volumes for prometheus + grafana
2. Check how configMap is being embededd into prometheus config - mount points, access, why smth is in /etc/ folder
3. You can name ports (pog)